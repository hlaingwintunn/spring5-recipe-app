spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sfg_prod?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    username: sfg_prod_user
    password: guru
    platform: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
    show-sql: false